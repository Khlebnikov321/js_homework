Вгадай результат / З описом
Корістувався іноді допомогою бо шось тяжко згадувалось/треба повертатися до перегляду лекції ще декілька разів))


'number' + 3 + 3 
'number33' 

// число буде перетворено на стрічку тому шо одиг із операндів є стрічкою а інший число 




null + 3
число 3

// додавання виконується з типами, які не є числом / спочатку перетворюються на число




5 && "qwerty"
"qwerty"

// && повертає друге значення якшо обидва значення true




+'40' + +'2' + "hillel";
буде рядок "42hillel"

// додавання числа 42 та рядка "hillel" приводить до перетворення числа на рядок,за допоимогую конкатенації




'10' - 5 === 6;
false

// строге порівняння!  === перевіряє як значення і тип даних
// якшо 5 не дорівнює 6 результат порівняння false 




true + false
1

// 1 - true а 0 - false / 1 + 0 = 1
// тому true



'4px' - 3
NaN

// тому шо з рядка 4px не можна зробити з число
// тому якшо відняти число 3 тобуде NaN




'4' - 3
1

// тут модна перетворити рядок 4 на число
// тому можемо відняти




'6' + 3 ** 0;
61

// + між рядком '6' і числом 1 призводить числа на рядок
// конкатенація рядків '6' + '1 буде '61' як рядок




12 / '6'
2

// (/) призводить до перетворення типів
// рядок '6' на число 6
// ділення 12/6 буде 2




'10' + (5 === 6);
'10false'

// (5 === 6) точно буде false
// тому + поеднуе '10' + false і робить з false рядок




null == ''
false

// null рівний null та undefined
// значить null == undefined буде true
// null не рівний пустому рядку тому мабуть false)))




3 ** (9 / 3);
27

// 9/3 буде 3
// 3 ** 3 наче 27



!!'false' == !!'true'
true

// рядки 'false' та 'true' будуть false тому при  !! вони станцть true
// !!'false' буде true та !!'true' буде true (жах))
// true == true це true


0 || '0' && 1
1

// спочатку виконуемо && а потім ||
// || повертає перше значення якшо воно fls true або останне якшо всі до цього false
// 0 це fals а 1 цу true і якшо 0 || 1 то поверне 1



(+null == false) < 1;
false

// + перед null робить з null на число 0
// == порівнює 0 і false і тому 0 == false буде true
// true 1 (fslse це 0) тому 1<1 не true 



false && true || true
true

// спочатку виконуемо && а потім || 
// && повертає перше значення якшо воно fls а значить false && true буде false
// || повертає перше значення якшо воно true або останне якшо всі до цього false




false && (false || true);
true

// спочатку виконуемо && а потім || 
// && повертає перше значення якшо воно fls а значить false && true буде false
// || повертає перше значення якшо воно true або останне якшо всі до цього false
// тому false || true поверне true - тобто останне коли всі інші спочатку false




(+null == false) < 1 ** 5;
false

// + перетворює null на число - тобто 0
// 0 == false буде true
// true це 1
// 1 ** 5 буде завжди 1
// тому якшо питаемо чи 1<1 то отримаємо false













